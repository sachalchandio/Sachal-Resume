---
title: "Fronters and closers, business rules in the sale form, and four new carriers"
description: "The month I modeled how deals actually get sold — fronter and closer roles with real constraints — wired order dates and product rules into every provider, and"
date: "2025-01-31"
updated: "2025-01-31"
kind: "monthly"
category: "Monthly"
tags: ["sales-model", "business-rules", "filters", "brightspeed", "angular"]
month: "2025-01"
repo: "both"
author: "Sachal Chandio"
stats_commits: 194
stats_backend: 43
stats_frontend: 151
---

The month I modeled how deals actually get sold — fronter and closer roles with real constraints — wired order dates and product rules into every provider, and brought Brightspeed, ADT, Breezeline, and Buckeye online.

## Fronters and closers as first-class roles

A sale isn't one person's work, so I modeled the fronter and closer explicitly. I added closers across the backend, handled the null-closer cases cleanly, and enforced the rule that the fronter and closer can't be the same person. A getAllEmployeesWithUserType query backs the agent and closer pickers on the frontend so the form only offers valid people.

- Closer added end to end with null-closer handling
- Fronter and closer constrained to different people
- Employee-by-user-type query feeding the closer/agent inputs

## Rules that live in the sale form

I moved real selling constraints into the data model. Order dates became required on every provider — with support for multiple order dates — and I rewrote the Xfinity sale DTO wholly to fit. The product logic is enforced too: you can't sell a phone line unless internet or TV is on the order first, so the form can't capture an invalid bundle.

- Required, multi-value order dates across all providers
- Phone requires internet or TV on the order
- Xfinity sale DTO reworked end to end

## Four carriers and a filter overhaul

Brightspeed, ADT, Breezeline, and Buckeye all came online this month, plus new Frontier speeds and a fresh disposition. Alongside them I started a proper per-provider filter system, getting the AT&T filters working as the template the others would follow so the sale lists are actually queryable.

- Brightspeed, ADT, Breezeline, and Buckeye onboarded
- New Frontier speed tiers and a new disposition
- AT&T filters working as the pattern for every provider

## Approvals and a typography reset

I built out the approvals area with its own tabs, page, and buttons so reviews have a home in the UI. I also did a typography reset — converting px to rem, unifying the font family, and normalizing sizes across the app — which paid off immediately in how consistent the dense sale screens read.

- Approvals tabs, page, and actions
- px-to-rem conversion with unified fonts and sizing
