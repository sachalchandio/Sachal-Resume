---
title: "Auth, role-based access, and onboarding AT&T as the second provider"
description: "The month Telelinkz grew teeth: I built encrypted auth with role-restricted mutations, brought AT&T online with bulk import, and started taking the dashboard's"
date: "2024-08-31"
updated: "2024-08-31"
kind: "monthly"
category: "Monthly"
tags: ["auth", "rbac", "at&t", "leads", "angular"]
month: "2024-08"
repo: "both"
author: "Sachal Chandio"
stats_commits: 150
stats_backend: 40
stats_frontend: 110
---

The month Telelinkz grew teeth: I built encrypted auth with role-restricted mutations, brought AT&T online with bulk import, and started taking the dashboard's design seriously.

## Security: auth and roles

I stood up the auth layer end to end. The Angular interceptor sends a bearer access token to the backend, and I encrypt the token at rest in localStorage while sending it decrypted on the wire, with token-expiry checks on both sides. On top of that I added role-based restrictions on mutations, so what you can change now depends on who you are, backed by an auth guard on the routes.

- HTTP interceptor with bearer auth and token-expiry checks
- Encrypted token storage, decrypted in transit
- Role-based restrictions on GraphQL mutations behind an auth guard

## AT&T comes online

AT&T became the second provider, and this time I leaned into bulk input — the importer ingests per-agent sale rows straight into the database, with a new enum set and entity changes to fit AT&T's shape. I built the resolver and per-agent import logic on both ends so the data lands clean.

- Bulk AT&T import with per-agent ingestion
- Provider-specific enums and entity changes
- Matching resolver and frontend import logic

## Interested-customer leads

I built out the interested-customer lead flow — new inputs and columns in the database, fresh DTOs and create methods that return proper promises, and a one-to-one relation tying a lead to its owner. The create-lead page and form came together on the frontend so reps could actually capture leads.

- Lead entity with new fields and a one-to-one owner relation
- Create-lead page and styled form

## Taking the design seriously

With the plumbing working, I did a full dashboard redesign pass — shared CSS files for consistency, restyled forms and tables, a customer-lead page that finally looked intentional, and quality-of-life touches like a copy button for CS numbers. Export logs and an audit-friendly view rounded it out.

- Common CSS foundation and a full dashboard restyle
- Copy-to-clipboard for support numbers
- Export logs
