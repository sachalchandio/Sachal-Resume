---
title: "Inventory assignment, background jobs with BullMQ, and complex sale filters"
description: "The month I gave Telelinkz an inventory system, moved heavy uploads onto BullMQ queues with a monitoring dashboard, and rebuilt sale filtering across every"
date: "2025-04-30"
updated: "2025-04-30"
kind: "monthly"
category: "Monthly"
tags: ["inventory", "bullmq", "queues", "filters", "employees"]
month: "2025-04"
repo: "both"
author: "Sachal Chandio"
stats_commits: 127
stats_backend: 37
stats_frontend: 90
---

The month I gave Telelinkz an inventory system, moved heavy uploads onto BullMQ queues with a monitoring dashboard, and rebuilt sale filtering across every provider so the data is finally fast to slice.

## Assigning inventory to people

I built the inventory assignment flow this month — an assign-inventory screen, an assigned-inventory tab in the table, and the validation checks to keep an assignment well-formed. It gives the team a real record of which units are in whose hands instead of tracking gear in someone's head.

- Assign-inventory screen and assigned-inventory tab
- Validation on inventory assignment

## Heavy uploads on a queue

Bulk uploads were doing too much work inline, so I moved them onto BullMQ. The jobs now run on a queue and process reliably, and I stood up the Bull dashboard for visibility into what's running and what failed — which meant solving the dashboard's auth so it isn't wide open. Uploads work properly now, with the queue absorbing the load.

- Bulk uploads processed through BullMQ jobs
- Bull monitoring dashboard with its auth sorted out

## Complex filters across every provider

I reworked sale filtering into a complex-filter system applied to all sale providers, including a sale-stage filter, and fixed the weekly logic to look back a true seven days from now. Along the way I made the default sale flag sold instead of unassigned, got number-of-lines calculating correctly, and converted loose option strings into enums so the filters are type-safe.

- Unified complex filters with a sale-stage filter for all providers
- Weekly window fixed to a real -7 days, default flag set to sold
- Number-of-lines logic and options migrated to enums

## Employee management and dashboard polish

On the people side I built out Add Employee and employee details, with a CNIC input and a dynamic gender field in the user form. The dashboard got percentages and color legends on the sale-status views, plus a cleaned-up disposition-approval form and table.

- Add Employee and employee details with CNIC and dynamic gender
- Dashboard percentages and color legends
- Disposition-approval form and table refresh
