# Render Blueprint — DISABLED (deployment handled manually, not via the API/Blueprint).
# To re-enable: uncomment the block below, then commit & push (or run a Blueprint deploy
# from render.com → New → Blueprint → connect this repo → Apply).
#
# services:
#   - type: web
#     name: sachal-portfolio
#     runtime: docker
#     dockerfilePath: ./Dockerfile
#     plan: free
#     healthCheckPath: /healthz
#     autoDeploy: true
#     envVars:
#       - key: PORT
#         value: "8000"
